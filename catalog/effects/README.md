# Catalog — Effects 分类

当前收录：`scale` / `fade` / `bounce`（详见 `../index.yaml`，实现于 `src/animation/`）。

规划：`slide` / `shake` / `flash` / `punch` / `rotate` / `glow`

收录标准：视觉反馈执行器（RefCounted 类），独立于交互与业务，参数化。
