# Catalog — Components 分类

当前收录：`ui_button` / `scale_button` / `toggle_button` / `base_panel` / `ui_popup` / `base_card`（详见 `../index.yaml`）。

收录标准：

- 可独立实例化的 UI 单元（场景 + 脚本 + README）
- 与 Gameplay 解耦（零业务依赖）
- 有 Demo 与 Catalog 条目
